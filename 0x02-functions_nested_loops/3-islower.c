#include "main.h"

/**
 *_islower - checks is a character is lower case or not
 *@c: is the char we check
 *Return: 1 if the character is lowercase
 */

int _islower(int c)
{
	return (c >= 'a' && c <= 'z');
}
